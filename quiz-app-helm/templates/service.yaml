apiVersion: v1
kind: Service
metadata:
  name: {{ include "quiz-app-helm.fullname" . }}
  labels:
    {{- include "quiz-app-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quiz-app-helm.selectorLabels" . | nindent 4 }}
